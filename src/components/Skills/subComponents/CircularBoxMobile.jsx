import styled from "@emotion/styled";
import { CircularBox } from "./";

const CircularBoxMobile = styled(CircularBox)(({ theme }) => ({
    width: "4.5rem",
    height: "4.5rem",
}));

export default CircularBoxMobile;
